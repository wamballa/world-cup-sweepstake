# Build Phases

`/backlog.md` is the single source of truth for phase sequencing, work item IDs, and implementation status.

Do not maintain a second phase list in this document. If phase sequencing changes, update `/backlog.md` and record the decision in `/planning/decision-log.md`.
